{% extends 'base.html.twig' %}

{% block content %}

    <div class="container order-bg">
        <h1 class="text-center p-3">Mes commandes</h1>
        {% for order in orders %}
        <div class="row w-75 m-auto text-center">
            <div class="col"><a href="{{ path('order_content', { 'id': order.id }) }}">Commande n°{{ order.id }}</a></div>
            <div class="col">{{ order.dateTime|date('d-m-Y') }}</div>
            <div class="col">{{ order.price|number_format(2) }} &euro;</div>
            <div class="col">{{ order.paymentMethod|capitalize }}</div>
        </div>
        {% endfor %}
    </div>

{% endblock %}