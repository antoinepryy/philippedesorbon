{% extends 'base.html.twig' %}

{% block content %}
    <div class="text-center p-5" style="min-height: calc(100vh - 300px)">
        <p>Vous allez être redirigé vers le module de paiement crédit agricole,<br> une fois
        la commande validée, vous recevrez un mail<br> et nous vous enverrons votre commande
        dans les plus brefs délais</p>
        <img style="width: 20%;"  class="m-3" src="{{ asset('ressource/image/crca/secured_payment.jpg') }}" alt="Paiement sécurisé garanti par Crédit Agricol"/>
        <form method="POST" action="{{ serveurOK }}">
            <input type="hidden" name="PBX_SITE" value="{{ pbx_site }}">
            <input type="hidden" name="PBX_RANG" value="{{ pbx_rang }}">
            <input type="hidden" name="PBX_IDENTIFIANT" value="{{ pbx_identifiant }}">
            <input type="hidden" name="PBX_TOTAL" value="{{ pbx_total }}">
            <input type="hidden" name="PBX_DEVISE" value="978">
            <input type="hidden" name="PBX_CMD" value="{{ pbx_cmd }}">
            <input type="hidden" name="PBX_PORTEUR" value="{{ pbx_porteur }}">
            <input type="hidden" name="PBX_REPONDRE_A" value="{{ pbx_repondre_a }}">
            <input type="hidden" name="PBX_RETOUR" value="{{ pbx_retour }}">
            <input type="hidden" name="PBX_EFFECTUE" value="{{ pbx_effectue }}">
            <input type="hidden" name="PBX_ANNULE" value="{{ pbx_annule }}">
            <input type="hidden" name="PBX_REFUSE" value="{{ pbx_refuse }}">
            <input type="hidden" name="PBX_HASH" value="SHA512">
            <input type="hidden" name="PBX_TIME" value="{{ dateTime }}">
            <input type="hidden" name="PBX_SHOPPINGCART" value="{{ htmlspecialchars(pbx_shopping_cart) }}">
            <input type="hidden" name="PBX_BILLING" value="{{ htmlspecialchars(pbx_billing) }}">
            <input type="hidden" name="PBX_SOUHAITAUTHENT"  value="{{ pbx_souhaitauthent }}">
            <input type="hidden" name="PBX_HMAC" value="{{ pbx_hmac }}">
            <input class="btn btn-primary" type="submit" value="Aller vers le module de paiement">
        </form>
    </div>


{% endblock %}